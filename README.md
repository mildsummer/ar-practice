# AR Practice

## install

installing modules

```
$ yarn install
```

### run on localhost

```sh
$ yarn start
```
run a local server `/.dist` as root.

---

## production build

```bash
$ yarn production
```

created in `/dist` directory.
